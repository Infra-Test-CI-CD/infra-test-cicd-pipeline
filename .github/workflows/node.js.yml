 # This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]

jobs:
#Build Started
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build
      
  # Save the build output as an artifact so deploy job can use it
    - name: Upload production build
      uses: actions/upload-artifact@v4
      with:
        name: next-build
        path: out

# Deploy to S3 + Invalidate CloudFront

  deploy:
    runs-on: ubuntu-latest
    needs: build   # 👈 Waits until build job is finished
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Download build from previous job
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: next-build
          path: out

      - name: Deploy to S3
        uses: jakejarvis/s3-sync-action@v0.5.1
        with:
          args: --delete
        env:
          AWS_S3_BUCKET: ${{ secrets.infra-ci-cd-bucket }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AKIA4ULAQABK5SGPMNWA }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.tSgdCepJ/Iw+vjtGhM5DH30bYuItf6os2SuxkKUk }}
          AWS_REGION: ${{ secrets.ap-south-1 }}
          SOURCE_DIR: out

      - name: Invalidate CloudFront cache
        uses: chetan/invalidate-cloudfront-action@v2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AKIA4ULAQABK5SGPMNWA }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.tSgdCepJ/Iw+vjtGhM5DH30bYuItf6os2SuxkKUk }}
          DISTRIBUTION: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          PATHS: "/*"
